                    <!-- Search Navbar -->
                    <div class="hidden md:flex flex-1 max-w-lg">
                        <form action="{{ route('front.index') }}" method="GET" class="w-full relative group">
                            <div class="relative w-full text-slate-400 focus-within:text-brand-500">
                                <div class="absolute inset-y-0 left-0 flex items-center pl-4 pointer-events-none">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                                </div>
                                <input type="text" name="search" value="{{ request('search') }}" placeholder="Cari 'lomba essay nasional'..." class="block w-full bg-slate-50 border border-slate-200 rounded-full pl-11 pr-4 py-2.5 text-sm focus:ring-brand-500 focus:border-brand-500 text-slate-700 placeholder-slate-400">
                            </div>
                        </form>
                    </div>

                    <!-- Auth Links -->
                    <div class="flex items-center space-x-4">
                        @if (Route::has('login'))
                            @auth
                                <a href="{{ url('/dashboard') }}" class="px-6 py-2.5 bg-brand-500 hover:bg-brand-600 text-white font-bold rounded-xl transition-colors">Dashboard</a>
                            @else
                                <a href="{{ route('login') }}" class="px-6 py-2.5 bg-brand-500 hover:bg-brand-600 text-white font-bold rounded-xl shadow-md transition-colors shadow-brand-500/30">Login</a>
                            @endauth
                        @endif
                    </div>
                </div>
                
                <!-- Category Pills -->
                <div class="flex overflow-x-auto py-4 gap-3 no-scrollbar border-t border-slate-100">
                    <a href="{{ route('front.index') }}" class="shrink-0 px-6 py-2 rounded-full font-bold text-sm transition-colors {{ !request('category_id') ? 'bg-brand-500 text-white shadow-md shadow-brand-500/30' : 'bg-white border border-slate-200 text-slate-600 hover:bg-slate-50' }}">
                        Semua Event
                    </a>
                    @foreach($categories as $cat)
                        <a href="{{ route('front.index', ['category_id' => $cat->id]) }}" class="shrink-0 px-6 py-2 rounded-full font-bold text-sm transition-colors {{ request('category_id') == $cat->id ? 'bg-brand-500 text-white shadow-md shadow-brand-500/30' : 'bg-white border border-slate-200 text-slate-600 hover:bg-slate-50' }}">
                            {{ $cat->name }}
                        </a>
                    @endforeach
                </div>
            </div>
        </nav>

        <!-- Hero Section -->
        <main class="relative z-10">
            <!-- Background Gradient for Hero -->
            <div class="absolute top-0 left-0 w-full h-[400px] bg-gradient-to-b from-brand-50 to-surface -z-10"></div>
            
            <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 pt-16 pb-20 text-center">
                <h2 class="text-4xl md:text-6xl font-extrabold tracking-tight text-slate-800 mb-6 leading-tight">
                    Temukan <br>
                    <span class="text-brand-500">Event & Lomba</span> <br>
                    Terbaikmu!
                </h2>
                <p class="mt-4 text-lg text-slate-600 mx-auto mb-10 leading-relaxed font-medium">
                    Lebih dari <span class="text-brand-500 font-bold">500+</span> event essay, olahraga, seminar, dan workshop siap kamu ikuti!
                </p>
                
                <!-- Big Search Bar -->
                <form action="{{ route('front.index') }}" method="GET" class="max-w-2xl mx-auto bg-white p-2 rounded-2xl shadow-xl shadow-brand-500/5 flex items-center border border-slate-200 focus-within:ring-2 focus-within:ring-brand-500/50">
                    <div class="flex-1 relative flex items-center text-slate-400 focus-within:text-brand-500">
                        <div class="absolute inset-y-0 left-0 flex items-center pl-4 pointer-events-none">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                        </div>
                        <input type="text" name="search" value="{{ request('search') }}" placeholder="Ketik 'lomba essay' atau 'seminar AI'..." class="block w-full border-transparent focus:border-transparent focus:ring-0 text-slate-700 bg-transparent py-3 pl-12 pr-3 text-base md:text-lg placeholder-slate-400">
                    </div>
                    <button type="submit" class="px-8 py-3.5 bg-brand-500 hover:bg-brand-600 text-white font-bold rounded-xl transition-colors shrink-0">
                        Cari Event
                    </button>
                </form>
